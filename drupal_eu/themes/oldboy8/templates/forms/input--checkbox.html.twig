{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
{{ children }}
#}
<input{{ attributes }} /><label class="form__label-checkbox" for="{{ attributes.id }}">I agree to<br> <a class="form__policy" href="{{ path('entity.node.canonical', {'node': 3})   }}">PRIVACY POLICY</a></label>
